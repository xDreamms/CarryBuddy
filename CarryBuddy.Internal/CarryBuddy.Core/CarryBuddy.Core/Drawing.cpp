#include "stdafx.h"

/*ID3DXLine* Line;
IDirect3DDevice9* Device;

void Draw::Init(IDirect3DDevice9* DeviceT) {
	Device = DeviceT;
	D3DXCreateLine(Device, &Line);
}

void Draw::DrawLine(float x1, float y1, float x2, float y2, float width, const char r, const char g, const char b) {
	DWORD Color = D3DCOLOR_XRGB(r, g, b);
	D3DXVECTOR2 LineVertexes[] = { D3DXVECTOR2(x1, y1), D3DXVECTOR2(x2, y2) };

	Line->SetWidth(width);
	Line->Begin();
	Line->Draw(LineVertexes, 2, Color);
	Line->End();
}*/